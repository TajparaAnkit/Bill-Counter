/**
 * Single source of truth for the app's brand name shown on invoices.
 * Change this one value to rebrand every invoice (header, UPI payee, footer, PDF).
 */
export const BRAND_NAME = 'Bill Counter';
